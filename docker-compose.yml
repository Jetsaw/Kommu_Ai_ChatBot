version: "3.9"

services:
  kai:
    build: .
    restart: unless-stopped
    ports:
      - "6090:8000"
    env_file: .env
    environment:
      - PORT=8000
    volumes:
      - ./logs:/app/logs
      - ./media:/app/media
      - ./sessions.db:/app/sessions.db
